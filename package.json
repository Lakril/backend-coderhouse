{
    "name": "backend-jack",
    "module": "app.js",
    "type": "module",
    "devDependencies": {
        "bun-types": "latest"
    },
    "scripts": {
        "clean": "rm -rf dist && echo 'Done.'",
        "dev": "bun --watch run app.js"
    },
    "peerDependencies": {
        "typescript": "^5.0.0"
    },
    "dependencies": {
        "express": "^4.18.2",
        "prettier": "^3.0.3"
    }
}
